{
  "name": "selectbeam",
  "displayName": "SelectBeam",
  "description": "Send code to terminal AI or browser AI in Firefox, Edge, Chrome, Brave — same tab every time. No copy-paste.",
  "version": "0.0.7",
  "publisher": "NoahMenezes",
  "license": "MIT",
  "icon": "icon.png",
  "repository": "https://github.com/NoahMenezes/SelectBeam.git",
  "homepage": "https://github.com/NoahMenezes/SelectBeam#readme",
  "bugs": {
    "url": "https://github.com/NoahMenezes/SelectBeam/issues"
  },
  "author": {
    "name": "Noah Menezes"
  },
  "keywords": [
    "ai",
    "clipboard",
    "productivity",
    "context",
    "terminal",
    "browser"
  ],
  "categories": [
    "AI"
  ],
  "engines": {
    "vscode": ">=1.85.0"
  },
  "main": "./dist/extension.js",
  "activationEvents": [
    "onStartupFinished",
    "onCommand:selectbeam.sendSelection",
    "onCommand:selectbeam.sendToBrowser",
    "onCommand:selectbeam.chooseTarget",
    "onCommand:selectbeam.showBridgeStatus"
  ],
  "contributes": {
    "commands": [
      {
        "command": "selectbeam.sendSelection",
        "title": "SelectBeam: Send Selection to AI"
      },
      {
        "command": "selectbeam.sendToBrowser",
        "title": "SelectBeam: Send Selection to Browser AI (ChatGPT / Claude / Gemini / DeepSeek)"
      },
      {
        "command": "selectbeam.chooseTarget",
        "title": "SelectBeam: Choose Send Target"
      },
      {
        "command": "selectbeam.showBridgeStatus",
        "title": "SelectBeam: Show Browser Bridge Status"
      }
    ],
    "keybindings": [
      {
        "command": "selectbeam.sendSelection",
        "key": "ctrl+alt+a",
        "mac": "cmd+alt+a",
        "when": "editorHasSelection"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "selectbeam.sendSelection",
          "when": "editorHasSelection",
          "group": "navigation"
        },
        {
          "command": "selectbeam.sendToBrowser",
          "when": "editorHasSelection",
          "group": "navigation"
        }
      ]
    },
    "walkthroughs": [
      {
        "id": "selectbeam.getStarted",
        "title": "SelectBeam in 60 seconds",
        "description": "Highlight code, send it to AI, same tab every time. No token, no setup.",
        "steps": [
          {
            "id": "selectbeam.step.send",
            "title": "Send your first snippet",
            "description": "Highlight any code and press Ctrl+Alt+A (Cmd+Alt+A on Mac). Pick an AI once — your pick is remembered from then on.",
            "media": {
              "markdown": "walkthroughs/step-send.md"
            },
            "completionEvents": [
              "onCommand:selectbeam.sendToBrowser"
            ]
          },
          {
            "id": "selectbeam.step.companion",
            "title": "Link your browser (once)",
            "description": "Recommended: install from Edge Add-ons (Edge) or Firefox AMO (Firefox/Zen) — links in Details. Brave/Chrome use Chrome Web Store or Load unpacked browser/ folder. Then just keep a chat tab open.",
            "media": {
              "markdown": "walkthroughs/step-companion.md"
            }
          },
          {
            "id": "selectbeam.step.reuse",
            "title": "Send again — same tab",
            "description": "Every further send refills the same chat tab automatically. No new tabs, no pasting, never auto-submits.",
            "media": {
              "markdown": "walkthroughs/step-reuse.md"
            }
          }
        ]
      }
    ],
    "configuration": {
      "title": "SelectBeam",
      "properties": {
        "selectbeam.defaultTarget": {
          "type": "string",
          "enum": [
            "auto",
            "clipboard",
            "terminal"
          ],
          "default": "auto",
          "description": "Where your code goes. Auto tries the terminal first and copies if needed. Clipboard always just copies. Terminal always uses the terminal."
        },
        "selectbeam.rememberTerminalChoice": {
          "type": "boolean",
          "default": true,
          "description": "Remember which terminal you picked so you are asked once, not every time."
        },
        "selectbeam.askForPrompt": {
          "type": "boolean",
          "default": true,
          "description": "Ask for a short note after your code, for example 'explain this'. Leave it empty to send code only. SelectBeam never presses Enter for you."
        },
        "selectbeam.agentStartDelayMs": {
          "type": "number",
          "default": 2000,
          "description": "How long to wait after starting an AI tool before putting code in, so the tool has time to open. In milliseconds."
        },
        "selectbeam.agentCommands": {
          "type": "object",
          "default": {},
          "description": "If one of your AI tools starts with a different command, write it here, for example { \"copilot\": \"gh copilot\" }."
        },
        "selectbeam.defaultBrowser": {
          "type": "string",
          "enum": [
            "ask",
            "last",
            "chatgpt",
            "claude",
            "gemini",
            "deepseek",
            "grok",
            "copilot"
          ],
          "default": "last",
          "description": "Which browser AI the shortcut sends to. Last reuses your last pick (ask once, then automatic). Ask shows a list every time. Or fix one. The palette command always lets you pick."
        },
        "selectbeam.rememberBrowserChoice": {
          "type": "boolean",
          "default": true,
          "description": "Remember your browser pick so Last works."
        },
        "selectbeam.systemBrowser": {
          "type": "string",
          "enum": [
            "ask",
            "last",
            "system",
            "firefox",
            "edge",
            "chrome",
            "chromium",
            "brave"
          ],
          "default": "ask",
          "description": "Which browser app opens the chat. Ask shows Firefox / Edge picker every time. Last reuses your last app. Or fix one."
        },
        "selectbeam.rememberSystemBrowserChoice": {
          "type": "boolean",
          "default": true,
          "description": "Remember your browser app pick so Last works."
        },
        "selectbeam.browserUrls": {
          "type": "object",
          "default": {},
          "description": "If a chat site address changes, write it here, for example { \"deepseek\": \"https://chat.deepseek.com/\" }."
        },
        "selectbeam.reuseBrowserTab": {
          "type": "boolean",
          "default": true,
          "description": "Reuse the already-linked browser AI tab instead of opening a new tab every time. Needs the free SelectBeam browser companion (Firefox first). Falls back to opening the site if no live tab."
        },
        "selectbeam.liveTabTTLMinutes": {
          "type": "number",
          "default": 60,
          "description": "How long a linked browser tab is considered fresh. After this, SelectBeam opens the site again instead of reusing. In minutes."
        },
        "selectbeam.bridgeEnabled": {
          "type": "boolean",
          "default": true,
          "description": "Run the local browser bridge (localhost HTTP, no internet needed) so the browser companion can report its live tab and pick up code. Turn off if you only use copy+open."
        },
        "selectbeam.bridgePort": {
          "type": "number",
          "default": 51337,
          "description": "Localhost port for the browser bridge. Change only if this port is taken on your machine."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "node ./esbuild.js --production",
    "compile": "bun ./esbuild.js",
    "watch": "bun ./esbuild.js --watch",
    "package": "bun ./esbuild.js --production",
    "package-vsix": "vsce package --allow-missing-repository",
    "publish:vscode": "vsce publish --allow-missing-repository",
    "publish:openvsx": "ovsx publish --allow-missing-repository",
    "publish:all": "bun run package && vsce publish --allow-missing-repository --packagePath ./selectbeam-0.0.7.vsix && ovsx publish ./selectbeam-0.0.7.vsix",
    "check-types": "tsc --noEmit",
    "format": "bunx prettier --write .",
    "format:check": "bunx prettier --check .",
    "clean": "rm -rf dist",
    "labels": "bash scripts/setup-labels.sh",
    "test": "echo \"No tests yet (V1)\" && exit 0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.85.0",
    "esbuild": "^0.28.2",
    "ovsx": "^1.2.0",
    "typescript": "^5.3.3"
  }
}
